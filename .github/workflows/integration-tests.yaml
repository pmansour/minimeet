name: Integration tests
# Run on any push to any branch, on any PR, and every 12 hours.
on:
  push:
  pull_request:
  schedule:
    - cron: '0 */12 * * *'
jobs:
  run-e2e-test:
    environment: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '16'
      - name: Prepare test credentials
        shell: bash
        env:
          GOOGLE_USERNAME: ${{ secrets.GOOGLE_USERNAME }}
          GOOGLE_PASSWORD: ${{ secrets.GOOGLE_PASSWORD }}
        run: |
          printf '{
            "username": "%s",
            "password": "%s"
          }' "$GOOGLE_USERNAME" "$GOOGLE_PASSWORD" > test/test-creds.json
      - run: npm ci
      - name: Install xvfb
        run: sudo apt install xvfb --yes --quiet
      - run: xvfb-run npm test
